# Candy Corn overnight handoff, September 2, 2026

For the Nyx high-level planner (Sol) watching the build tonight, and for Ethan in the morning. Everything here is current as of 11 PM Central.

## Where we are

| Phase | State | Where |
|---|---|---|
| 0. Clickable prototype | Approved by Ethan | https://candy-corn-mvp-prototype.pages.dev, `apps/candycorn-prototype/` |
| 1. SwiftUI shell | Merged to main, builds and tests on iPhone 17 simulator, 24 native captures committed | `apps/candycorn-ios/`, `apps/candycorn-ios/screenshots/` |
| 2. Vault and real capture | Running on Nyx as plan `v_fZAMoAhlWP` | branch `nyx-plan/v_fZAMoAhlWP`, worktree `.worktrees/plan-v_fZAMoAhlWP` |
| 3. Organizer AI through OpenRouter | Queued on Nyx as plan `cNnDFr3tESHf`, starts when Phase 2 completes | |
| Front end redesign | Approved mockups, to be implemented by Claude after Phase 3 | `docs/design/v2/` and https://candy-corn-ios-gallery.pages.dev/screens/ |

Nyx moderator: solanav2-alpha on `http://localhost:8790`. Plan status: `curl -s localhost:8790/plan/<id>`. Report when complete: `curl -s localhost:8790/plan/<id>/report`.

## Division of labor, decided tonight

- Nyx (Sol plans, Codex workers build) owns Phase 2 and Phase 3: the encrypted vault, real audio recording, camera, search, export, the OpenRouter organizer, and the wiring between them.
- Claude owns the front end. After Phase 3 merges and is verified, Claude rebuilds the screens to the approved mockups on top of Nyx's services. Nyx workers should therefore treat the current SwiftUI views as functional scaffolding: make them work, keep them honest, do not spend effort on visual polish beyond the Phase 2 defect list.

## What Phase 2 must deliver (accept only with evidence)

1. The seven shell defects in the Phase 2 goal fixed, with tests: settings tabs switch in place, check-in bands set the value on tap and drag, one back control per screen, no pill around the mood bands, secondary text at or above 4.5 to 1 contrast, no shell-meta copy, app icon present.
2. GRDB vault with SQLCipher (or the documented fallback seam), key in Keychain, migrations, FTS5.
3. Real voice recording through AVAudioSession and AVAudioRecorder, saved to disk before anything else, playback works, interruptions stop and save, background audio mode for appointment recording.
4. Camera capture saving the photo as the immutable source.
5. History and search reading from the vault; export folder and delete everything.
6. Build and full test suite pass on the iPhone 17 simulator; 24 screenshots recaptured; COMPARISON.md updated with real results.

## What Phase 3 must deliver

1. Provider protocols from spec section 23; `OpenRouterLanguageModel` with model ids as configuration (`deepseek/deepseek-v4-flash-0731` text, `deepseek/deepseek-v4-flash-vision-exp` photos), completion budget at least 1200 tokens, retry once on empty content.
2. Key entered in Settings, stored in Keychain, never in the bundle, logs, or exports. Never read `~/.nyx` from app code.
3. Rewrite, summary, talking-point suggestions, candidate goals with Add, Edit, Ignore; photo to text; prep brief; what-leaves-this-device sheet before every send.
4. Evidence-span validation: any suggestion whose evidence is not verbatim in the source is dropped.
5. Tests with a fake transport only; no live calls in the suite.

## How to verify the voice path (Sol, run this on the host, not inside a worker sandbox)

Workers cannot reach CoreSimulatorService; the acceptance capture must run on the host.

```sh
cd apps/candycorn-ios
/opt/homebrew/bin/xcodegen generate
xcodebuild -project CandyCorn.xcodeproj -scheme CandyCorn -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/cc-dd CODE_SIGNING_ALLOWED=NO build test
open -a Simulator
xcrun simctl boot 'iPhone 17' ; xcrun simctl bootstatus 'iPhone 17' -b
xcrun simctl install booted /tmp/cc-dd/Build/Products/Debug-iphonesimulator/CandyCorn.app
xcrun simctl launch --terminate-running-process booted dev.candycorn.app -screen /journal/voice
```

Then in the Simulator window: tap Talk, speak into the Mac microphone for ten seconds, tap Stop, confirm "Saved on this device", tap play and hear it back. The simulator uses the Mac microphone by default (Simulator menu, I/O, Audio Input). If `simctl launch` hangs for every app including Safari, the simulator service is wedged; the fix is `xcrun simctl shutdown all` then `killall -9 com.apple.CoreSimulator.CoreSimulatorService`, which Ethan has to run.

What cannot be verified on the Mac and stays open for the real iPhone: recording while locked, phone-call interruptions, AirPods route changes.

## Known traps from today

- A worker committed a 366 MB `.pnpm-store` into the repo. It is gitignored now; refuse any commit that adds `node_modules`, `.pnpm-store`, `DerivedData`, `build/`, or `.build`.
- A worker left a literal placeholder in `pnpm-workspace.yaml`. Read generated config files before committing them.
- Prototype disclaimers leaked into product copy ("Simulated recording"). Product copy only; test data may be fictional but the words on screen are real.
- Nyx's crash recovery drops plans under 50 percent complete if the moderator dies. Do not restart the alpha moderator while a plan is mid-flight.
- The simulator service on this Mac wedged once today. Symptoms and fix above.

## Do not

- Do not start the front-end redesign in Nyx. Claude does that tomorrow against `docs/design/v2/`.
- Do not modify `docs/SPEC.md`, the prototype, or `docs/design/v2/`.
- Do not bundle, print, log, or commit any key. The OpenRouter key stays in `~/.nyx/secrets` for the host and in the phone's Keychain for the app.
- Do not add analytics, tracking, or any third-party SDK beyond GRDB, SQLCipher, and later FluidAudio.

## Tomorrow, in order

1. Ethan reviews Phase 2 and Phase 3 results on the simulator and, if signing is set up, on his iPhone.
2. Claude rebuilds Today, Journal, Goals, History, Settings, and Talk to the approved mockups, keeps Nyx's services, retargets the tab bar to Goals, Journal, Today, History, Settings, and moves Prepare behind the appointment card.
3. Phase 4 (appointment audio, transcription, diarization, session debrief) is submitted to Nyx once Ethan has a consented practice recording to test with.
